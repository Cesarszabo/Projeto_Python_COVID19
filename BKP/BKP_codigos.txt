## Rascunhos dos testes feitos na analise_covid19

# df = pd.read_csv(url, parse_dates=["ObservationDate", "Last Update"], dtype={"Confirmed":"Int32","Deaths":"Int32","Recovered":"Int32"})

# df = df.rename(columns={"SNo":"id", "ObservationDate":"dataobservacao", "Province/State": "estado", "Country/Region":"pais","Last Update":"dataatualizacao", "Confirmed":"casosconfirmados", "Deaths":"mortes","Recovered":"recuperados"})

# print(df_Brasil["WHO_region"].unique())

***Criando coluna e Função lambda que calcula a diferença entre um dia e o seu antecessor

df_Brasil['novoscasos'] = list(map(
    lambda x: 0 if (x==0) else df_Brasil['Cumulative_cases'].iloc[x] - df_Brasil['Cumulative_cases'].iloc[x-1],
    np.arange(df_Brasil.shape[0])))

iloc -> faz a procura pelo indice
df_brasil.shape[0] -> seleciona apenas as linhas
map -> traz os interaveis e como está numa lista ele acumula